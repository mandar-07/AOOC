#include <stdio.h>
#include <stdlib.h>

int main(void){
    int n;
    int factorial;

    puts("Please Enter the Number: ");
    scanf("%d", &n);

    if(n < 0){
        puts("Factorial of negative number is not possible");
        exit(EXIT_FAILURE);
    }

    
}